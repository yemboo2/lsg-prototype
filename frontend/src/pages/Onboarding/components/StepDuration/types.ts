export enum EPresetType {
  VAR1 = 'var1',
  VAR2 = 'var2',
  VAR3 = 'var3',
  INDIVIDUAL = 'ind',
}

export interface IDurations {
  work: number;
  activity: number;
  mental: number;
  break: number;
  recap: number;
}
