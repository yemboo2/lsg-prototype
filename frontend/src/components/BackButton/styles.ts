import styled from '@emotion/styled';

export const Container = styled.div`
  cursor: pointer;

  .back-page {
    position: absolute;
    left: 15px;
    margin-top: -40px;
    height: 30px;
    width: 30px;
  }
`;
